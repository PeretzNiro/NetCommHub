// Represents a message signaling the need for a menu update
package com.netcommhub.common.message;

public class UpdateMenu extends Carrier {

    // Constructor for UpdateMenu class
    public UpdateMenu() {
        // Call the superclass constructor (Carrier) to initialize common fields
        super();
    }
}