// Represents a message requesting a name from the user
package com.netcommhub.common.message;

public class NameRequest extends Carrier {

    // Constructor for NameRequest class
    public NameRequest() {
        // Call the superclass constructor (Carrier) to initialize common fields
        super();
    }
}