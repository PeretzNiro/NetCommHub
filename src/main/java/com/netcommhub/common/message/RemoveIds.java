// Represents a message indicating the need to remove certain IDs
package com.netcommhub.common.message;

public class RemoveIds extends Carrier {

    // Constructor for RemoveIds class
    public RemoveIds() {
        // Call the superclass constructor (Carrier) to initialize common fields
        super();
    }
}